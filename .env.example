# Database connection — override these for AWS RDS or non-default local setup.
# Local Docker defaults are used when unset.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=itf_tournament
DB_USER=itfuser
DB_PASSWORD=itfpwd
